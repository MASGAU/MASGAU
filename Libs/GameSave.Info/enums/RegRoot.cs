
namespace GameSaveInfo {
    public enum RegRoot {
        classes_root,
        current_user,
        current_config,
        dyn_data,
        local_machine,
        performace_data,
        users
    }
}
